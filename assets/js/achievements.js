// Script for the stars achievements

let topicAchievements = [Science, History, Music, Art, Geography, Sports];
console.log(parseInt(localStorage.getItem(topic + "_stars", stars) + 0));

for(let i in topicAchievements) {
    let numberStars = (parseInt(localStorage.getItem(topicAchievements[i] + "_stars", stars) + 0))


l